<%= form_with(model: talk_room, local: true) do |form| %>
  <% if talk_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(talk_room.errors.count, "error") %> prohibited this talk_room from being saved:</h2>

      <ul>
      <% talk_room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :member %>
    <%= form.text_field :member %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
